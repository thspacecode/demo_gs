[project]
name = "gs_battery"
authors = [
    { name = "GS Battery Demo", email = "demo@gsbattery.example.com" }
]
description = "Customizations for the GS Battery procurement demo"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0"
erpnext = ">=16.0.0"
